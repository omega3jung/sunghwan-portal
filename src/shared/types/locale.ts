/** Required fallback locale for localized application values. */
export type DefaultLocale = "en";
/** Locale codes currently supported by application translations. */
export type Locale = DefaultLocale | "es" | "fr" | "ko";

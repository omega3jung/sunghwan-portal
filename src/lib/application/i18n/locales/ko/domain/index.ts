// src/lib/application/i18n/locales/ko/domain/index.ts

import enumLocale from "./enum.json";

const domain = {
  enum: enumLocale,
};

/** Korean domain-specific translation namespaces. */
export default domain;
